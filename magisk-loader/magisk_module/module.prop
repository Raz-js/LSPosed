id=${moduleId}
name=${api} - LSPosed - Raz Fork
version=${versionName} (${versionCode})
versionCode=${versionCode}
author=Raz, ${authorList}
description=Cli-enabled fork of LSPosed
updateJson=${updateJson}
